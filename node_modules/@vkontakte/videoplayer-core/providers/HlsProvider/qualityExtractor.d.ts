import { Kbps, IRectangle } from '@vkontakte/videoplayer-shared';
import { VideoQuality } from "../../utils/quality/types";
export declare type ManifestUrl = {
    id: string;
    quality: VideoQuality;
    url: string;
    bandwidth?: Kbps;
    size?: IRectangle;
    fps?: number;
};
declare const extractQualities: (masterManifestUrl: string, baseUrl?: string) => Promise<ManifestUrl[]>;
export default extractQualities;
