import { IDashSource } from "../../player/types";
import { IProvider, IProviderParams } from '../types';
declare type Params = IProviderParams<IDashSource> & {
    config: {
        cacheDuration: number;
    };
};
export default class DashProvider implements IProvider {
    private videoState;
    private subscription;
    private video;
    private buffer;
    private dash;
    private representations$;
    private currentRepresentation$;
    private params;
    private textTracksManager;
    private elementSize$;
    private dashLiteEvents;
    private log;
    constructor(params: Params);
    private subscribe;
    destroy(): void;
    private prepare;
    private handleManifestReady;
    private handleRepresentationPlay;
    private selectRepresentation;
    private seek;
    private playIfAllowed;
    private syncPlayback;
}
export {};
