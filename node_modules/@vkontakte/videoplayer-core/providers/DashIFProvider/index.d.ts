import { IDashURLSource, VideoFormat } from "../../player/types";
import { IProvider, IProviderParams } from "../types";
import { Milliseconds } from '@vkontakte/videoplayer-shared';
interface IParams extends IProviderParams<IDashURLSource> {
    format: VideoFormat;
    config: {
        maxPausedTime: Milliseconds;
    };
}
export default class DashIFProvider implements IProvider {
    private subscription;
    private videoState;
    private video;
    private player;
    private params;
    private textTracksManager;
    private videoTracks;
    private frameRatesByFrameHeight;
    private isLive$;
    private maxSeekBackTime$;
    private availableFrom$;
    private elementSize$;
    private liveOffset;
    private destroyBigRequest;
    constructor(params: IParams);
    private loadDashJs;
    private init;
    private subscribe;
    private stop;
    private prepare;
    private seek;
    private play;
    private pause;
    private syncPlayback;
    destroy(): void;
}
export {};
