import { MpegParser, Segment } from "./types";
import { Byte } from '@vkontakte/videoplayer-shared';
interface Box {
    id: string;
    size: Byte;
    contents: DataView;
}
export declare const parseBox: (view: DataView) => Box;
export declare const parseBoxes: (view: DataView) => Box[];
export declare const validateHeader: (view: DataView) => boolean;
export declare const parseSegments: (view: DataView) => Segment[];
export declare const parser: MpegParser;
export {};
