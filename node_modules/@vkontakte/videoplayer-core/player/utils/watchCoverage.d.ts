import { IObservable, IValueObservable, Seconds, IRange } from '@vkontakte/videoplayer-shared';
import { IStateMachine } from "../../utils/StateMachine/types";
import { PlaybackState, SeekState } from "../types";
interface IParams {
    playbackState: IStateMachine<PlaybackState>;
    seekState: IStateMachine<SeekState>;
    playbackAbort$: IObservable<void>;
    looped$: IObservable<Seconds>;
    position$: IValueObservable<Seconds | undefined>;
}
export declare const watchCoverage: ({ playbackState, seekState, playbackAbort$, looped$, position$, }: IParams) => IObservable<IRange<number>>;
export {};
