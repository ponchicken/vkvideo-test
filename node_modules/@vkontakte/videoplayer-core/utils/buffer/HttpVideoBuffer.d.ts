import { Seconds } from '@vkontakte/videoplayer-shared';
import { IVideoBuffer } from './types';
export declare const DEFAULT_GAP_TOLERANCE: Seconds;
export default class HttpVideoBuffer {
    private _buffer;
    private _source;
    constructor(source: HTMLVideoElement);
    fill(): IVideoBuffer[];
    getByTime(time: number): IVideoBuffer | undefined;
    getNextWithGap(buf: IVideoBuffer, gapTolerance?: number): IVideoBuffer | undefined;
    getNext(currentBuffer: IVideoBuffer): IVideoBuffer | undefined;
    smartRemove(till: number, after: number, removeFn: (a: number, b: number) => void): void;
    destroy(): void;
}
